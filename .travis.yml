---
dist: focal
language: ruby

cache: bundler

before_install: gem install bundler -v 2.1.2

rvm:
  - 2.4
  - 2.5
  - 2.6
  - 2.7
  - ruby-head

matrix:
  fast_finish: true
  allow_failures:
    - rvm: ruby-head
